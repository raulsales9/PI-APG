{% extends 'layout.html.twig' %}
    {% block content %}
    <div class="container">
	<h3>Usuario {{detalleClient["name"]}}</h3>
	<article class="eventList mb-3">
 		<span class="fs-3 me-3">{{detalleClient["name"]}}, {{detalleClient["surnames"]}}</span>
		<span class="text-muted ms-3"> {{detalleClient["email"]}} </span>
		<span class="text-muted ms-3"> {{detalleClient["phone"]}} </span>
		<span class="text-muted ms-3">- {{detalleClient["roles"]}} -</span>
		<div class="float-end">
			<a class="btn btn-warning" href="/twig/updateUser/{{detalleClient["id"]}}">Editar Usuario</a>
			<a class="btn btn-danger" href="/twig/deleteUser/{{detalleClient["id"]}}">Borrar Usuario</a>
		</div>
			<details class="mt-3"> 
			<summary>Ficheros </summary>
			<form method="post" action="">
				{% for task in detalleClient["files"] %}
				 	
					<div class="ms-3 mb-3">
					<input type="checkbox" class="form-check-input" id="exampleCheck1" name="{{task["idFile"]}}" {{task["isSubmited"]}}>
					{{task["name"]}}, {{task["type"]}}
					
					{# <label class="form-check-label" for="exampleCheck1">Entregado</label> #}
					</div> 
					<br>
				{% endfor %}
			</form>
			</detail>

	</article>
	</div>
{# 	<nav>
		<ul>
			<li>ID Usuario : {{detalleClient["id"]}}</li>
			<li>Nombre : {{detalleClient["name"]}}</li>
			<li>Apellidos : {{detalleClient["surnames"]}}</li>
			<li>Correo Electrónico : {{detalleClient["email"]}}</li>
			<li>Rol  : {{detalleClient["roles"]}}</li>
 			
			{% for task in detalleClient["files"] %}
				<li>Archivos : {{task["idFile"]}}, {{task["name"]}}, {{task["type"]}}</li> 
			{% endfor %}	 
			<li>Telefono : {{detalleClient["phone"]}}</li>
			{% for task in detalleClient["events"] %}
 				<li>Eventos : {{task["id"]}},  {{task["name"]}},  {{task["place"]}}</li> 
			{% endfor %}
			
		</ul>
	</nav> #}
		
	</div>
{% endblock %}